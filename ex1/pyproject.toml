[tool.poetry]
name = "the-matrix-ex1"
version = "1.0.0"
description = ""
authors = ["sadeen"]

[tool.poetry.dependencies]
python = ">=3.10"
numpy = "^2.5.0"
pandas = "^3.0.3"
matplotlib = "^3.11.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"